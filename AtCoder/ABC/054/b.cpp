#include <iostream>
using namespace std;
const int NM_MAX = 50;
int main (void) {
    int N;
    int M;
    string A[NM_MAX];
    string B[NM_MAX];

    cin >> N >> M;
    for (int i = 0; i < N; i++) cin >> A[i];
    for (int i = 0; i < M; i++) cin >> B[i];

    
    return 0;
}